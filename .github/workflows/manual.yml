name: Manual Test Workflow

on:
  workflow_dispatch:

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello Mama 🔥 This is manual workflow"

      - name: Show date
        run: date
